<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Generátor hesel s obrázkem</title>
  <style>
    body { 
      font-family: Arial, sans-serif; 
      text-align: center; 
      padding: 50px 20px; 
      max-width: 800px; 
      margin: 0 auto; 
    }
    #password-container {
      display: flex;
      justify-content: center;
      align-items: center;
      margin: 20px auto;
      max-width: 300px;
      border: 1px solid #ccc;
      border-radius: 4px;
      overflow: hidden;
    }
    #password { 
      flex-grow: 1;
      padding: 10px;
      font-size: 16px;
      border: none;
      outline: none;
    }
    #copy-button {
      background: none;
      border: none;
      padding: 10px;
      cursor: pointer;
      font-size: 20px;
    }
    #image-container {
      margin-top: 20px;
      min-height: 300px;
    }
    #image { 
      max-width: 100%; 
      height: auto; 
      max-height: 400px;
    }
    .error { 
      color: red; 
    }
    #generate-button {
      padding: 10px 20px;
      font-size: 16px;
      cursor: pointer;
      margin: 20px 0;
    }
  </style>
</head>
<body>
  <h1>Generátor hesel s obrázkem</h1>
  <div id="password-container">
    <input type="text" id="password" value="<%= password %>" readonly>
    <button id="copy-button" onclick="copyPassword()">📋</button>
  </div>
  <button id="generate-button" onclick="location.reload()">Generovat nové heslo a obrázek</button>
  <div id="image-container">
    <% if (imageUrl) { %>
      <img id="image" src="<%= imageUrl %>" alt="Náhodný obrázek" onerror="this.onerror=null;this.src='https://via.placeholder.com/400x300?text=Image+Not+Available';">
    <% } else { %>
      <p class="error">Nepodařilo se načíst obrázek. Zkuste to prosím znovu.</p>
    <% } %>
  </div>

  <script>
    function copyPassword() {
      const passwordElement = document.getElementById('password');
      passwordElement.select();
      document.execCommand('copy');
      alert('Heslo bylo zkopírováno do schránky!');
    }
  </script>
</body>
</html>